{
	"name": "wagov-dtt base devcontainer",
	"build": {
		"dockerfile": "Dockerfile",
		"context": "..",
		"args": {
			"GITHUB_TOKEN": "${localEnv:GITHUB_TOKEN}"
		}
	},
	"features": {
		"ghcr.io/devcontainers/features/docker-in-docker:2": {}
	}
}